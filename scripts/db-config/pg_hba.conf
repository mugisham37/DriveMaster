# PostgreSQL Client Authentication Configuration
# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections only
local   all             postgres                                peer
local   all             all                                     scram-sha-256

# IPv4 local connections:
host    all             postgres        127.0.0.1/32            scram-sha-256
host    all             postgres        ::1/128                 scram-sha-256

# Application connections
host    adaptive_learning    app_user        0.0.0.0/0               scram-sha-256
host    adaptive_learning    readonly_user   0.0.0.0/0               scram-sha-256
host    adaptive_learning    migration_user  0.0.0.0/0               scram-sha-256

# Replication connections
host    replication     replicator      0.0.0.0/0               scram-sha-256

# Development/Docker connections (less restrictive for local development)
host    all             all             172.16.0.0/12           scram-sha-256
host    all             all             192.168.0.0/16          scram-sha-256
host    all             all             10.0.0.0/8              scram-sha-256

# Deny all other connections
host    all             all             0.0.0.0/0               reject